export function removeHTMLTag() {
  // str = str.replace(/<\/?[^>]*>/g, ""); //去除HTML tag
  // str = str.replace(/[ | ]*\n/g, "\n"); //去除行尾空白
  // str = str.replace(/\n[\s| | ]*\r/g, "\n"); //去除多余空行
  // str = str.replace(/&nbsp;/gi, ""); //去掉&nbsp;
  // str = str.replace(/<[^>]+>|&[^>]+;/g, "").trim(); //去掉所有的html标签和&nbsp;之类的特殊符合
  // return str;

  return 2121;
}
